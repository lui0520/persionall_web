// ============================================================
//  Hero 頁面配置文件
//  修改此文件即可調整首頁版面，無需動到元件程式碼
// ============================================================

const heroConfig = {

  // ──────────────────────────────────────────────
  //  文字內容
  // ──────────────────────────────────────────────
  text: {
    /** 頂部小標（英文標籤列） */
    label: 'Magician · Director · Creator',

    /** 主標題（支援 \n 換行） */
    title: '人生就像魔術\n需要一點彈性',

    /** 副標題 */
    subtitle: '最具親和力的幽默創作魔術師',
  },

  // ──────────────────────────────────────────────
  //  按鈕
  // ──────────────────────────────────────────────
  buttons: {
    primary: {
      /** 按鈕文字 */
      label: '查看經歷',
      /** 點擊後跳至哪個頁面（對應 App.jsx 的 pages 鍵值） */
      page: 'experience',
    },
    secondary: {
      label: '合作洽談',
      page: 'contact',
    },
  },

  // ──────────────────────────────────────────────
  //  左側文字區塊 — 間距
  // ──────────────────────────────────────────────
  leftPadding: {
    /**
     * 左側留白（clamp 語法：最小值, 隨視窗縮放, 最大值）
     * 數值越大文字越往右
     */
    left:  'clamp(1.25rem, 10vw, 10rem)',
    /** 右側留白（文字區塊與圖片之間的距離） */
    right: 'clamp(1rem, 3vw, 3rem)',
    /** 上下留白 */
    vertical: 'py-20',
  },

  // ──────────────────────────────────────────────
  //  字體大小
  // ──────────────────────────────────────────────
  fontSize: {
    /**
     * 主標題字體大小（clamp 語法：最小, 隨視窗縮放, 最大）
     * 建議範圍：2rem ~ 8rem
     */
    title:    'clamp(2.6rem, 6vw, 5rem)',
    /** 副標題字體大小 */
    subtitle: 'clamp(1.1rem, 2vw, 1.4rem)',
  },

  // ──────────────────────────────────────────────
  //  右側圖片相框
  // ──────────────────────────────────────────────
  image: {
    /** 圖片檔名（位於 public/images/web/ 資料夾） */
    file: 'hero/hero_05.jpg',

    /**
     * 相框寬度（佔整個頁面寬度的比例）
     * 格式：'lg:w-[38%] xl:w-[40%]'
     * 數值越小圖片越窄，左側文字空間越多
     */
    frameWidth: 'lg:w-[38%] xl:w-[40%]',

    /**
     * 相框在頁面上的水平位移（translateX）
     * 負值 → 相框整體向左移
     * 正值 → 相框整體向右移
     * 範例：'-8%' / '20px' / '0'
     */
    frameTranslateX: '-8%',

    /**
     * 相框高度（clamp 語法）
     * 第一個值：手機最小高度
     * 第二個值：隨視窗高度縮放（vh = 視窗高度百分比）
     */
    frameHeight: 'clamp(300px, 80vh, 80vh)',

    /**
     * 圖片垂直偏移 %
     * 正值 → 圖片向下移（顯示下半部）
     * 負值 → 圖片向上移（顯示上半部，讓臉部更居中）
     * 範圍建議：-50 ~ 50
     */
    offsetY: -5,

    /**
     * 圖片水平偏移 %
     * 正值 → 圖片向右移
     * 負值 → 圖片向左移
     * 範圍建議：-50 ~ 50
     */
    offsetX: 10,

    /**
     * 是否顯示圖片左側漸層淡化效果
     * true  → 圖片左緣漸漸融入白色背景
     * false → 圖片左緣直接切齊，無淡化
     */
    fadeLeft: true,
  },

  // ──────────────────────────────────────────────
  //  動畫
  // ──────────────────────────────────────────────
  animation: {
    /**
     * 各元素依序出現的間隔秒數
     * 數值越大每個元素出現越慢
     */
    stagger: 0.13,

    /** 向上淡入動畫的持續秒數 */
    upDuration: 0.95,

    /** 淡入動畫的持續秒數（用於圖片、裝飾線） */
    fadeDuration: 1.2,

    /** 向上淡入的起始偏移距離（px），數值越大動畫幅度越大 */
    upY: 32,
  },

}

export default heroConfig
